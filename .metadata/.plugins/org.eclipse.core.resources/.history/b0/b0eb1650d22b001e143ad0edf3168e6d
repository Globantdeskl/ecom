package com.app.inter;

import java.util.Arrays;
import java.util.HashSet;
import java.util.List;
import java.util.Set;
import java.util.stream.Collectors;

public class Test {

	public static void main(String[] args) {

		String str = "Nauuurendar";
		List<String> myList = Arrays.asList(str);
		String strr = "";
		Set<String> items = new HashSet<>();
		List<String> lh =  myList.stream().filter(n -> !set.add(n)).collect(Collectors.toList());;
		// Storing it inside array of strings

		for (int i = 0; i < ls.size(); i++)
			strr += ls.get(i);
		System.out.print(strr);
	}

}
