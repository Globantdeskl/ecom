envVars:
  ENV: "prod"
  SPRING_PROFILES_ACTIVE: "kubernetes"
  ACI_DIVISION_NUMBER: "ae_ugp_rs"
  ACI_CLIENT_ID: "000450030001"
  ACI_SSL_KEY: "20171"
  ACI_CONFIG_PATH: "/app/resources/data/kubernetes/prod"
  FRAUD_ENABLED: "true"
  NEW_RELIC_APP_NAME: "checkout-fraud-check-prod"

#Environment name
env: prod

#Deployment values
deployment:
  replicas: 1

#Workload identity enablement for the GCP API access
workloadIdentity:
  enabled: false
  gsa_name:
  gcp_project_id: aeo-gke-internal-prod-8fad

#Horizontal Pod autoscaling options
autoscaling:
  enabled: true
  maxReplicas: 2
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

#Ingress values
ingress:
  enabled: true
  domain: int-pr.ae-gcp.io
